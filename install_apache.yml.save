---
- hosts: all
  become: true
  tasks:

  - name: install the apache2 and php package for Debian
    apt:
      name:
        - apache2
        - libapache2-mod-php
        -
      state: latest
      update_cache: yes
    when: ansible_distribution == "Debian"

